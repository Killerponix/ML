#Daten um 0.0  0.1 , 0.1 und 1.1 erzeugen
#d=0.5 also 0,25 0,25, Daten verschieben
#Merkmale sind die koordinaten
#0.0 und 1.1 sind klasse 1 1.0 und 01 sind klasse 0
#Zwei objekte sollen x und y sein ??? Ignorieren? 4 Objekte, also xtest, xtrain, ytrain, ytest
#Aufgabe 2 hat ungefährt 70%
#Übung 6.3 für Praktikum   Split von daten in Prraktikum Neuronen activation epochen altes Modell laden oder neu
# batch size l2_reg
import numpy as np
import matplotlib.pyplot as plt

def XORData(d):
    X = np.random.rand(500,2)
    print(X11)


    # fig=plt.figure()
    # ax=fig.add_subplot(1,1,1)
    # ax.scatter(X)
    # fig.show()

XORData(1)

